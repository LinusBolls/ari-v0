How to create a new pair type

1. Create a cs file in the directory PairTypes
2. the class must derive from Backend.Objects.Pair
3. add the pairtype name to MainForm/Bearbeiten(tabPageEditMapping)/Typ(comboBoxPairType), must be the same as the type string
4. Search for comments that start with TODO_NEWPAIRTYPE and follow instructions